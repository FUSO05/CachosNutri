{
  "name": "cachosnutri",
  "version": "1.0.0",
  "private": true,
  "description": "CachosNutri — planeamento alimentar profissional para nutricionistas",
  "scripts": {
    "test": "playwright test",
    "test:ui": "playwright test --ui"
  },
  "devDependencies": {
    "@playwright/test": "^1.48.0"
  }
}
